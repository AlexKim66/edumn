<template>
    <div >

        <Tabs>
            <TabPane label="Талбар" icon="ios-list">

                <div>

                    <h5 class="ve-title">Утга /values/</h5>
                    <div>
                        <draggable class="dragArea" v-model="tableFields.values" :options="{group:'element'}">

                            <div  v-for="(column, index) in tableFields.values" :key="index" class="data-element">

                                <input v-model="column.title"  :placeholder="column.name" @blur="column.editing = false"  ></input>
                                <Button class="delete-field" size="small" @click="deleteTitle(index)">
                                    <Icon type="close"/>
                                </Button>
                            </div>
                        </draggable>
                    </div>
                  
                </div>

            </TabPane>
            <TabPane label="Тохиргоо" icon="android-options">
                <div>
                    <i-switch>
                        <span slot="open">Том</span>
                        <span slot="close">Жижиг</span>
                    </i-switch>
                </div>
            </TabPane>

        </Tabs>

    </div>

</template>

<script>

    import draggable from 'vuedraggable';



    import {mapGetters} from 'vuex';

    export default {
        methods: {
            showTable(table) {
                console.log('showing ', table)
            },


            deleteValues(index) {

                this.tableFields.values.splice(index, 1);
            },

        },
        mounted() {

        },
        data() {
            return {
                filters: [],

            }
        },
        computed: {
            ...mapGetters({
                elementTypes: 'elementTypes',
                elementType: 'elementType',
                tableFields: 'tableFields',
            })
        },
        components: {
            draggable,

        },



    };
</script>

