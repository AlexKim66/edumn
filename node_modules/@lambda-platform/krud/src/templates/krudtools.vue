<template>
    <div class="crud-page-header-right-inside">
        <!--<Tooltip content="Устсан мэдээлэл харах">-->
        <!--<Button ghost class="crud-tool" icon="eye-disabled" @click="refreshGrid"></Button>-->
        <!--</Tooltip>-->
        <Tooltip content="Хадгалах">
            <a @click="$props.save" class="btnLine" v-if="isSave">
                <i class="ti-save"></i>
            </a>
        </Tooltip>

        <Tooltip content="Дахин ачааллах">
            <a @click="$props.refresh" class="btnLine" v-if="isRefresh">
                <i class="ti-reload"></i>
            </a>
        </Tooltip>

        <Tooltip content="Хэвлэх" v-if="isPrint">
            <a @click="$props.print" class="btnLine">
                <i class="ti-printer"></i>
            </a>
        </Tooltip>

        <Tooltip content="Excel файлаар татах" v-if="isExcel">
            <a class="btnLine" href="javascript:void(0)" v-if="$props.exportLoading" >
                <Spin>
                    <Icon type="ios-loading" size=18 class="spin-icon-load"></Icon>
                </Spin>
            </a>
            <a @click="$props.exportExcel" v-else class="btnLine">
                <i class="ti-download"></i>
            </a>
        </Tooltip>

        <form @submit="searchGrid">
            <div class="right-search">
                <input v-model="searchModel" :placeholder="$static_words ? $static_words.search : 'Хайх...'" class="right-search-input"/>
                <i class="ti-search"></i>
            </div>
        </form>
    </div>
</template>

<script>
export default {
    props: ["refresh", "exportExcel", "print", "search", "save", "options", "isExcel", "isPrint", "isRefresh", "isSave", "exportLoading"],
    data() {
        return {
            searchModel: null
        }
    },
    methods: {
        searchGrid(e) {
            e.preventDefault();
            this.$props.search(this.searchModel);
        },
    }
};
</script>
